import { {{$CONST_NAME_MODEL$}}Resource } from '@larajs/cms/api/{{$API_VERSION$}}';
import { AxiosRequestConfig } from 'axios';

export * from './table';
export * from './form';
export function use{{$NAME_USES$}}() {
  const list = (query, props?: AxiosRequestConfig) => {{$CONST_NAME_MODEL$}}Resource.list(query, props);
  const store = (data, props?: AxiosRequestConfig) => {{$CONST_NAME_MODEL$}}Resource.store(data, props);
  const show = (id, props?: AxiosRequestConfig) => {{$CONST_NAME_MODEL$}}Resource.show(id, props);
  const update = (id, data, props?: AxiosRequestConfig) => {{$CONST_NAME_MODEL$}}Resource.update(id, data, props);
  const destroy = (id, props?: AxiosRequestConfig) => {{$CONST_NAME_MODEL$}}Resource.destroy(id, props);

  return {
    list,
    store,
    show,
    update,
    destroy,
  };
}
